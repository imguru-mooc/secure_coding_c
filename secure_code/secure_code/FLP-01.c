#if 0
#include <stdio.h>

int main() {
    float f = 1.0f / 3.0f;
    printf("Float is %.40f\n", f);

    return 0;
}
#endif 

#if 0
#include <stdio.h>

int main() {
    double a = 3.0;
    double b = 7.0;
    double c = a / b;

    if (c == (a / b))
        printf("same\n");
    else
        printf("not same\n");

    return 0;
}
#endif 
